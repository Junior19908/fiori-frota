<mvc:View
  controllerName="com.skysinc.frota.frota.controller.Main"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:table="sap.ui.table"
  displayBlock="true">

  <Page id="page" showHeader="false">
    <content>

      <!-- Barra superior: título + filtro período + limpar -->
      <OverflowToolbar>
        <core:Icon src="sap-icon://car-rental" size="1.5rem"/>
        <Title text="Gestão de Frota" level="H3"/>
        <ToolbarSpacer/>

        <!-- Período (obrigatório no OData – o controller garante default = ontem) -->
        <Label text="Período:">
          <layoutData>
            <OverflowToolbarLayoutData priority="NeverOverflow"/>
          </layoutData>
        </Label>
        <DateRangeSelection
            id="drs"
            displayFormat="long"
            width="18rem"
            change=".onFilterChange">
          <layoutData>
            <OverflowToolbarLayoutData priority="NeverOverflow"/>
          </layoutData>
        </DateRangeSelection>

        <Button text="Limpar" type="Transparent" press=".onClearFilters"/>
      </OverflowToolbar>

      <!-- KPIs -->
      <OverflowToolbar width="100%">
        <ObjectStatus text="Gasto em combustível" icon="sap-icon://expense-report" state="Information"/>
        <ObjectNumber number="{kpi>/gastoCombustivelFmt}" state="Information"/>
        <ToolbarSeparator/>
        <ObjectStatus text="Litros totais" icon="sap-icon://measure" state="Warning"/>
        <ObjectNumber number="{kpi>/totalLitrosFmt}" state="Warning"/>
        <ToolbarSeparator/>
        <ObjectStatus text="Custo material/serviço" icon="sap-icon://wrench" state="Success"/>
        <ObjectNumber number="{kpi>/custoMateriaisFmt}" state="Success"/>
        <ToolbarSeparator/>
        <ObjectStatus text="Preço médio" icon="sap-icon://expense-report" state="None"/>
        <ObjectNumber number="{kpi>/precoMedioFmt}" unit="R$/L" state="None"/>
      </OverflowToolbar>

      <!-- Filtros -->
      <OverflowToolbar>
        <Text text="Filtro por Categorias:">
          <layoutData>
            <OverflowToolbarLayoutData priority="NeverOverflow"/>
          </layoutData>
        </Text>

        <ComboBox id="segCat" selectionChange=".onFilterChange" width="20rem" selectedKey="__ALL__">
          <items>
            <core:Item key="__ALL__" text="Todas"/>
            <!-- Demais itens preenchidos no onInit -->
          </items>
        </ComboBox>

        <Text text="Filtro por Veículo:">
          <layoutData>
            <OverflowToolbarLayoutData priority="NeverOverflow"/>
          </layoutData>
        </Text>

        <ComboBox id="inpVeiculo" selectionChange=".onFilterChange" width="12rem" selectedKey="__ALL__">
          <items>
            <core:Item key="__ALL__" text="Todos"/>
            <!-- Demais itens preenchidos no onInit -->
          </items>
        </ComboBox>

        <ToolbarSpacer/>
      </OverflowToolbar>

      <!-- Tabela principal -->
      <table:Table
          id="tbl"
          selectionMode="None"
          visibleRowCount="22"
          alternateRowColors="true"
          enableColumnReordering="true"
          width="auto"
          noDataText="Sem dados para o período/filtragem."
          rows="{ path:'/veiculos', sorter:{ path:'dataRef', descending:true } }">

        <table:columns>

          <table:Column sortProperty="veiculo" width="10rem">
            <Label text="Veículo"/>
            <table:template>
              <Text text="{veiculo}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="categoria" width="16rem">
            <Label text="Categoria"/>
            <table:template>
              <Text text="{categoria}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="descricao" width="18rem">
            <Label text="Descrição do veículo"/>
            <table:template>
              <Text text="{descricao}" maxLines="2"/>
            </table:template>
          </table:Column>

          <table:Column width="12rem" hAlign="Center">
            <Label text="Ações"/>
            <table:template>
              <HBox alignItems="Center" justifyContent="Center">
                <Button icon="sap-icon://detail-view" type="Transparent" tooltip="Materiais/Serviços" press=".onOpenMateriais"/>
                <Button icon="sap-icon://mileage" type="Transparent" tooltip="Abastecimentos" press=".onOpenAbastecimentos"/>
                <Button icon="sap-icon://insurance-car" type="Transparent" tooltip="Histórico" press=".onOpenHistorico"/>
              </HBox>
            </table:template>
          </table:Column>

          <!-- Agregados do período -->
          <table:Column sortProperty="custoMaterialAgg" width="12rem" hAlign="End">
            <Label text="Custo material/serviço (R$)"/>
            <table:template>
              <Text text="{path:'custoMaterialAgg', formatter:'.formatter.fmtBrl'}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="combustivelLitrosAgg" width="9rem" hAlign="End">
            <Label text="Combustível (L)"/>
            <table:template>
              <Text text="{path:'combustivelLitrosAgg', formatter:'.formatter.fmtLitros'}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="combustivelValorAgg" width="9rem" hAlign="End">
            <Label text="Combustível (R$)"/>
            <table:template>
              <Text text="{path:'combustivelValorAgg', formatter:'.formatter.fmtBrl'}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="custoTotalAgg" width="10rem" hAlign="End">
            <Label text="Custo total (R$)"/>
            <table:template>
              <Text text="{
                path: 'custoTotalAgg',
                formatter: '.formatter.fmtBrl'
              }"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="kmRodadosAgg" width="9rem" hAlign="End">
            <Label text="Km rodados"/>
            <table:template>
              <Text text="{path:'kmRodadosAgg', formatter:'.formatter.fmtKm'}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="hrRodadosAgg" width="9rem" hAlign="End">
            <Label text="Hr rodados"/>
            <table:template>
              <Text text="{path:'hrRodadosAgg', formatter:'.formatter.fmtNum'}"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="funcaokmcomb" width="8rem" hAlign="End">
            <Label text="Km/L"/>
            <table:template>
              <Text
                visible="{= ${kmRodadosAgg} > 0 }"
                text="{
                  parts:['kmRodadosAgg','combustivelLitrosAgg'],
                  formatter: '.formatter.fmtFuncaoKmComb'
                }"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="funcaohrRodados" width="8rem" hAlign="End">
            <Label text="L/Hr"/>
            <table:template>
              <Text
                visible="{= ${hrRodadosAgg} > 0 }"
                text="{
                  parts:['combustivelLitrosAgg','hrRodadosAgg'],
                  formatter: '.formatter.fmtFuncaoHoraComb'
                }"/>
            </table:template>
          </table:Column>

          <table:Column sortProperty="dataRef" width="9rem" hAlign="End">
            <Label text="Data"/>
            <table:template>
              <Text text="{path:'dataRef', formatter:'.formatter.fmtDate'}"/>
            </table:template>
          </table:Column>

        </table:columns>
      </table:Table>
    </content>
  </Page>
</mvc:View>
